// AUTOGENERATED by remote/libs/nats/subject-defs/src/generate.mjs
// Do not edit by hand; edit JSON Schema under schema/ and regenerate.

#![allow(clippy::needless_return)]

pub const NATS_CONTRACT_FINGERPRINT: &str = "sha256:2b00ab7021f06527d59d3446927332784657f7f3004af0462eda93f005c1687e";

// ---------- Static subjects ----------

/// Per-tick simulation frames fanned out for live demos (grid/agent snapshots and aggregate stats). Broadcast with no queue group so every interested consumer (and the websocket bridge) receives each frame. Default for AGENT_SIM_FRAME_SUBJECT.
/// Service: dd-agent-sim-server
pub const AGENT_SIM_FRAMES_SUBJECT: &str = "dd.remote.agent_sim.frames";

/// Inbound agent-based / cellular-automata simulation requests consumed by the agent-sim server. Subscribed with the dd-agent-sim-server queue group so requests load-balance across replicas. Default for AGENT_SIM_SIMULATE_SUBJECT.
/// Service: dd-agent-sim-server
pub const AGENT_SIM_SIMULATE_REQUESTS_SUBJECT: &str = "dd.remote.agent_sim.simulate.requests";
pub const AGENT_SIM_SIMULATE_REQUESTS_QUEUE_GROUP: &str = "dd-agent-sim-server";

/// Final simulation outcomes (final state summary, time series, convergence/extinction flags) emitted after a run completes. Carries an agent_sim.simulate.v1 envelope. Default for AGENT_SIM_RESULT_SUBJECT.
/// Service: dd-agent-sim-server
pub const AGENT_SIM_SIMULATE_RESULTS_SUBJECT: &str = "dd.remote.agent_sim.simulate.results";

/// Emitted when a Merkle root over a posting range is anchored to Solana (tamper-evidence notary). Carries tenant, anchor id, posting range/count, merkle root hex, tx signature, and slot.
/// Service: dd-billing-server
pub const BILLING_ANCHORS_SUBJECT: &str = "dd.remote.billing.anchors";

/// Provider-connection lifecycle feed (created, attached/activated, synced, failed). Carries tenant, connection id, provider tag, and the lifecycle transition. No credentials.
/// Service: dd-billing-server
pub const BILLING_CONNECTION_EVENTS_SUBJECT: &str = "dd.remote.billing.connections.events";

/// Emitted after a double-entry transaction is committed to the Postgres ledger. Carries a redacted summary (tenant, transaction id, kind, per-currency totals, posting count) so downstream services can react to ledger movement without reading the database. Default for BILLING_NATS_SUBJECT_LEDGER.
/// Service: dd-billing-server
pub const BILLING_LEDGER_POSTINGS_SUBJECT: &str = "dd.remote.billing.ledger.postings";

/// Emitted when a reconciliation break is opened during provider sync (drift between provider-reported state and the ledger). Carries tenant, provider, break type, currency, external ref, and expected/actual minor amounts. Alert-worthy.
/// Service: dd-billing-server
pub const BILLING_RECONCILIATION_BREAKS_SUBJECT: &str = "dd.remote.billing.reconciliation.breaks";

/// Inbound command requesting a provider-connection sync. Payload is {tenantId, connectionId}. Queue-grouped so billing-server replicas load-balance and a given command runs on exactly one pod. Default for BILLING_NATS_SUBJECT_SYNC_COMMANDS.
/// Service: dd-billing-server
pub const BILLING_SYNC_COMMANDS_SUBJECT: &str = "dd.remote.billing.commands.sync";
pub const BILLING_SYNC_COMMANDS_QUEUE_GROUP: &str = "dd-billing-server";

/// Redacted provider webhook receipt audit feed. Carries provider, external event id, event type, signature_ok, tenant/connection ids, and the payload sha256 prefix only. The raw webhook body and verification error detail are deliberately NOT published. Mirrors dd.remote.public_data.webhooks.events as an audit source, not a canonical store.
/// Service: dd-billing-server
pub const BILLING_WEBHOOK_RECEIPTS_SUBJECT: &str = "dd.remote.billing.webhooks.receipts";

/// Cross-chain bridge attestations: a source-chain lock confirmed read-only, signalling a transfer is ready for externally-signed destination release. Default for BLOCKCHAIN_BRIDGE_ATTESTATIONS_SUBJECT. Publish-only.
/// Service: dd-contract-service
pub const BLOCKCHAIN_BRIDGE_ATTESTATIONS_SUBJECT: &str = "dd.remote.blockchain.bridge.attestations";

/// Indexed on-chain references (Solana signatures / EVM tx hashes) emitted by the keyless blockchain indexing module when a watched address is polled. Default for BLOCKCHAIN_INDEX_EVENTS_SUBJECT. Publish-only.
/// Service: dd-contract-service
pub const BLOCKCHAIN_INDEX_EVENTS_SUBJECT: &str = "dd.remote.blockchain.index.events";

/// Monitoring-only MEV/arbitrage spread alerts emitted when an observed venue spread crosses the configured threshold. Default for BLOCKCHAIN_MEV_ALERTS_SUBJECT. Observation surface only; there is no execution path. Publish-only.
/// Service: dd-contract-service
pub const BLOCKCHAIN_MEV_ALERTS_SUBJECT: &str = "dd.remote.blockchain.mev.alerts";

/// Shared result fanout receiving every browser-job terminal result.
/// Service: dd-browser-job-runner
pub const BROWSER_JOB_RESULTS_SUBJECT: &str = "dd.remote.browser_jobs.results";

/// Redacted build lifecycle events (queued/running/succeeded/failed) published by the build server. Default for BUILD_SERVER_NATS_EVENT_SUBJECT.
/// Service: dd-build-server
pub const BUILD_SERVER_EVENTS_SUBJECT: &str = "dd.remote.build_server.events";

/// Redacted container-image registry events (ECR / docker registry webhook pushes) relayed by the build server. Default for BUILD_SERVER_NATS_IMAGE_SUBJECT.
/// Service: dd-build-server
pub const BUILD_SERVER_IMAGES_SUBJECT: &str = "dd.remote.build_server.images";

/// Durable build-request intake. Producers publish a build-server.v1 job document; build-server replicas consume via the shared queue group / durable JetStream consumer. Default for BUILD_SERVER_NATS_REQUEST_SUBJECT.
/// Service: dd-build-server
pub const BUILD_SERVER_REQUESTS_SUBJECT: &str = "dd.remote.build_server.requests";
pub const BUILD_SERVER_REQUESTS_QUEUE_GROUP: &str = "dd-build-server";

/// Terminal build results (succeeded/failed with jobId and error summary) for NATS-submitted and webhook-submitted jobs. Default for BUILD_SERVER_NATS_RESULT_SUBJECT.
/// Service: dd-build-server
pub const BUILD_SERVER_RESULTS_SUBJECT: &str = "dd.remote.build_server.results";

/// Per-fault lifecycle events (selected, injected, restored, aborted-by-guard) emitted by the chaos loops.
/// Service: dd-chaos
pub const CHAOS_EVENTS_SUBJECT: &str = "dd.remote.chaos.events";

/// Auditable chaos experiment records: each planned or executed fault (pod-kill, replica-jitter) is published here with its blast radius, target, and dry-run flag.
/// Service: dd-chaos
pub const CHAOS_EXPERIMENTS_SUBJECT: &str = "dd.remote.chaos.experiments";

/// Round-trip latency-probe subject. The chaos NATS probe publishes a ping and the same service replies, measuring request/reply RTT and jitter against the live cluster NATS server.
/// Service: dd-chaos
pub const CHAOS_PROBE_SUBJECT: &str = "dd.remote.chaos.probe";
pub const CHAOS_PROBE_QUEUE_GROUP: &str = "dd-chaos-probe";

/// Send-an-email requests consumed by dd-email-sms-contact-rs via the dd-email-sms-contact queue group (each request handled once across replicas). Payload: { to, subject, html, [text], [from] }.
/// Service: dd-email-sms-contact-rs
pub const CONTACT_EMAIL_SEND_SUBJECT: &str = "dd.remote.contact.email.send";
pub const CONTACT_EMAIL_SEND_QUEUE_GROUP: &str = "dd-email-sms-contact";

/// Send-a-push-notification request consumed by dd-email-sms-contact-rs via the dd-email-sms-contact queue group. Payload: { transport: webpush|fcm|expo|apns, [title], [body], [data], [token] (fcm/expo/apns device token), [subscription] (webpush { endpoint, keys: { p256dh, auth } }) }.
/// Service: dd-email-sms-contact-rs
pub const CONTACT_PUSH_SEND_SUBJECT: &str = "dd.remote.contact.push.send";
pub const CONTACT_PUSH_SEND_QUEUE_GROUP: &str = "dd-email-sms-contact";

/// Per-send result summaries published after each email/sms/push attempt (channel, recipient, ok, transport, upstream status). Carries no message bodies or secrets.
/// Service: dd-email-sms-contact-rs
pub const CONTACT_SEND_RESULTS_SUBJECT: &str = "dd.remote.contact.results";

/// Send-an-SMS requests consumed by dd-email-sms-contact-rs via the dd-email-sms-contact queue group. Payload: { to, body }.
/// Service: dd-email-sms-contact-rs
pub const CONTACT_SMS_SEND_SUBJECT: &str = "dd.remote.contact.sms.send";
pub const CONTACT_SMS_SEND_QUEUE_GROUP: &str = "dd-email-sms-contact";

/// Generic container pool request subject (legacy default; specific pools usually use ContainerPoolLanguageRequests with their own runtime prefix).
/// Service: dd-container-pool
pub const CONTAINER_POOL_REQUESTS_SUBJECT: &str = "dd.remote.container_pool.requests";

/// Generic container pool result subject.
/// Service: dd-container-pool
pub const CONTAINER_POOL_RESULTS_SUBJECT: &str = "dd.remote.container_pool.results";

/// Dispute resolution requests for solana.resolution.v1 envelopes (release-to-payee/refund-to-payer/split/award-to-claimant/uphold/overturn). Default for CONTRACT_RESOLVE_SUBJECT. Same NATS broadcast gating as ContractsSolanaSettle.
/// Service: dd-contract-service
pub const CONTRACTS_SOLANA_RESOLVE_SUBJECT: &str = "dd.remote.contracts.solana.resolve";
pub const CONTRACTS_SOLANA_RESOLVE_QUEUE_GROUP: &str = "dd-contract-service";

/// Published validation/simulation results. Default for CONTRACT_RESULT_SUBJECT.
/// Service: dd-contract-service
pub const CONTRACTS_SOLANA_RESULTS_SUBJECT: &str = "dd.remote.contracts.solana.results";

/// Settlement requests for solana.settlement.v1 envelopes (fund/release/refund/partial-release/split-release/dispute-award/expire/cancel). Default for CONTRACT_SETTLE_SUBJECT. NATS-initiated broadcast is off unless CONTRACT_NATS_SETTLEMENT_ENABLED=true; otherwise the service only validates, simulates, and confirms.
/// Service: dd-contract-service
pub const CONTRACTS_SOLANA_SETTLE_SUBJECT: &str = "dd.remote.contracts.solana.settle";
pub const CONTRACTS_SOLANA_SETTLE_QUEUE_GROUP: &str = "dd-contract-service";

/// Published settlement and dispute-resolution outcomes, including on-chain confirmation status. Default for CONTRACT_SETTLEMENT_RESULT_SUBJECT.
/// Service: dd-contract-service
pub const CONTRACTS_SOLANA_SETTLEMENT_RESULTS_SUBJECT: &str = "dd.remote.contracts.solana.settlement.results";

/// Validation requests for solana.contract.v1 instruction envelopes. Default for CONTRACT_VALIDATE_SUBJECT.
/// Service: dd-contract-service
pub const CONTRACTS_SOLANA_VALIDATE_SUBJECT: &str = "dd.remote.contracts.solana.validate";
pub const CONTRACTS_SOLANA_VALIDATE_QUEUE_GROUP: &str = "dd-contract-service";

/// Scheduled prompts published by dd-chron-service. A producer converts these into normal per-thread task messages so cron doesn't bypass idempotency or affinity.
/// Service: dd-remote-rest-api
pub const CRON_PROMPTS_SUBJECT: &str = "dd.remote.cron.prompts";
pub const CRON_PROMPTS_STREAM: &str = "DD_REMOTE_CRON";

/// Per-label annotation events emitted when a human annotator, model, or labeling function submits a label for a task item.
/// Service: dd-dataset-labeling
pub const DATASET_LABELING_LABEL_EVENTS_SUBJECT: &str = "dd.remote.dataset_labeling.label.events";

/// Spark/Airflow pipeline job intents that materialize aggregated gold labels into training datasets for downstream model training.
/// Service: dd-dataset-labeling
pub const DATASET_LABELING_PIPELINE_JOBS_SUBJECT: &str = "dd.remote.dataset_labeling.pipeline.jobs";

/// Aggregation, inter-annotator agreement, and gold-label export results from labeling runs.
/// Service: dd-dataset-labeling
pub const DATASET_LABELING_RESULTS_SUBJECT: &str = "dd.remote.dataset_labeling.results";

/// Inbound labeling task/annotation requests accepted over NATS. Payloads mirror the HTTP /tasks, /labels, and /functions/apply contracts.
/// Service: dd-dataset-labeling
pub const DATASET_LABELING_TASK_REQUESTS_SUBJECT: &str = "dd.remote.dataset_labeling.task.requests";
pub const DATASET_LABELING_TASK_REQUESTS_QUEUE_GROUP: &str = "dd-dataset-labeling";

/// Fan-out emitted on each evaluation of an enabled alert rule (disabled-rule evaluations are not published). Carries the rule id, title, current state (alerting/normal/no_data/error), and the triggering metric summary (observed value + condition) — never the underlying rows.
/// Service: dd-data-viz-rs
pub const DATA_VIZ_ALERTS_EVENTS_SUBJECT: &str = "dd.remote.dataviz.alerts.events";

/// Outbound notification-dispatch summaries published when an alert dispatch is processed. The dd-data-viz-notifiers queue group is reserved for a future notifier worker (no in-tree consumer yet); when added it will deliver each dispatch once. The payload is the dispatch summary (ids, status, counts) — the per-contact attempt list stays off this subject.
/// Service: dd-data-viz-rs
pub const DATA_VIZ_NOTIFICATIONS_DISPATCH_SUBJECT: &str = "dd.remote.dataviz.notifications.dispatch";
pub const DATA_VIZ_NOTIFICATIONS_DISPATCH_QUEUE_GROUP: &str = "dd-data-viz-notifiers";

/// Fan-out emitted when a publish request is saved or reviewed. Carries the publish-request summary (request/target ids, target title, requester, reviewer, status) — never the rendered payload or the free-text notes/review comment.
/// Service: dd-data-viz-rs
pub const DATA_VIZ_PUBLISH_EVENTS_SUBJECT: &str = "dd.remote.dataviz.publish.events";

/// Discrete-event simulation results. Default for DES_RESULT_SUBJECT.
/// Service: dd-ai-ml-pipeline
pub const DES_RESULTS_SUBJECT: &str = "dd.remote.des.results";

/// Discrete-event simulation job requests. Default for DES_SIMULATE_SUBJECT.
/// Service: dd-ai-ml-pipeline
pub const DES_SIMULATE_SUBJECT: &str = "dd.remote.des.simulate";
pub const DES_SIMULATE_QUEUE_GROUP: &str = "dd-des-simulator";

/// Document conversion requests consumed by dd-document-rs replicas.
/// Service: dd-document-rs
pub const DOCUMENT_CONVERT_REQUESTS_SUBJECT: &str = "dd.remote.document.convert";
pub const DOCUMENT_CONVERT_REQUESTS_QUEUE_GROUP: &str = "dd-document-rs";

/// Document conversion results published after a request completes or fails.
/// Service: dd-document-rs
pub const DOCUMENT_CONVERT_RESULTS_SUBJECT: &str = "dd.remote.document.results";

/// Inbound forecast/recommendation requests consumed by the economics server. Subscribed with the dd-economics-server queue group so requests load-balance across replicas. Default for ECONOMICS_FORECAST_REQUEST_SUBJECT.
/// Service: dd-economics-server
pub const ECONOMICS_FORECAST_REQUESTS_SUBJECT: &str = "dd.remote.economics.forecast.requests";
pub const ECONOMICS_FORECAST_REQUESTS_QUEUE_GROUP: &str = "dd-economics-server";

/// Forecast and recommendation results emitted by the economics server. Carries an economics.forecast.v1 envelope. Default for ECONOMICS_FORECAST_RESULT_SUBJECT.
/// Service: dd-economics-server
pub const ECONOMICS_FORECAST_RESULTS_SUBJECT: &str = "dd.remote.economics.forecast.results";

/// Market-event fan-out (narrative, event, and entity-level signals) published by the economics server for downstream consumers. Default for ECONOMICS_MARKET_EVENT_SUBJECT.
/// Service: dd-economics-server
pub const ECONOMICS_MARKET_EVENTS_SUBJECT: &str = "dd.remote.economics.market.events";

/// Published escrow validation results. Default for ESCROW_RESULT_SUBJECT.
/// Service: dd-escrow-rs
pub const ESCROW_SOLANA_RESULTS_SUBJECT: &str = "dd.remote.escrow.solana.results";

/// Validation requests for solana.escrow.v1 intents. Default for ESCROW_VALIDATE_SUBJECT.
/// Service: dd-escrow-rs
pub const ESCROW_SOLANA_VALIDATE_SUBJECT: &str = "dd.remote.escrow.solana.validate";
pub const ESCROW_SOLANA_VALIDATE_QUEUE_GROUP: &str = "dd-escrow-rs";

/// Lifecycle and per-epoch progress events from the distributed GA (best-fitness-so-far, convergence, epoch boundaries).
/// Service: dd-evolution-optimizer
pub const EVOLUTION_EVENTS_SUBJECT: &str = "dd.remote.evolution.events";
pub const EVOLUTION_EVENTS_STREAM: &str = "DD_REMOTE_EVOLUTION";

/// Per-island evolution jobs. The master publishes a subpopulation plus GA parameters for one epoch; island worker pods consume through the islands queue group and evolve it for N generations.
/// Service: dd-evolution-optimizer
pub const EVOLUTION_JOBS_SUBJECT: &str = "dd.remote.evolution.jobs";
pub const EVOLUTION_JOBS_QUEUE_GROUP: &str = "dd-evolution-optimizer-islands";
pub const EVOLUTION_JOBS_STREAM: &str = "DD_REMOTE_EVOLUTION";

/// Elite individuals migrating between islands on a ring topology between epochs. Carried inside the next epoch's jobs by the master; this subject lets observers tap migration flow.
/// Service: dd-evolution-optimizer
pub const EVOLUTION_MIGRANTS_SUBJECT: &str = "dd.remote.evolution.migrants";
pub const EVOLUTION_MIGRANTS_STREAM: &str = "DD_REMOTE_EVOLUTION";

/// Per-island evolution results. Islands publish the evolved (fitness-sorted) subpopulation and their best genome; the master aggregates them by solveId and epoch.
/// Service: dd-evolution-optimizer
pub const EVOLUTION_RESULTS_SUBJECT: &str = "dd.remote.evolution.results";
pub const EVOLUTION_RESULTS_STREAM: &str = "DD_REMOTE_EVOLUTION";

/// Hybrid assembly and process-decomposition requests for workers that split, combine, join, and sequence printed, milled, turned, sheet-cut, and postprocessed parts.
/// Service: dd-fabrication-server
pub const FABRICATION_ASSEMBLY_PLANNING_REQUESTS_SUBJECT: &str = "dd.remote.fabrication.assembly.planning.requests";
pub const FABRICATION_ASSEMBLY_PLANNING_REQUESTS_QUEUE_GROUP: &str = "dd-fabrication-assembly-planners";

/// Hybrid assembly and process-decomposition results carrying part splits, joins, process sequences, learning state, blockers, and release metadata.
/// Service: dd-fabrication-server
pub const FABRICATION_ASSEMBLY_PLANNING_RESULTS_SUBJECT: &str = "dd.remote.fabrication.assembly.planning.results";

/// Design input conversion requests for CAD, mesh, slicer, CAM setup, and neutral export workers.
/// Service: dd-fabrication-server
pub const FABRICATION_DESIGN_CONVERSION_REQUESTS_SUBJECT: &str = "dd.remote.fabrication.design.conversion.requests";
pub const FABRICATION_DESIGN_CONVERSION_REQUESTS_QUEUE_GROUP: &str = "dd-fabrication-design-converters";

/// Design input conversion results carrying neutral exports, translator evidence, blockers, and review metadata.
/// Service: dd-fabrication-server
pub const FABRICATION_DESIGN_CONVERSION_RESULTS_SUBJECT: &str = "dd.remote.fabrication.design.conversion.results";

/// Design synthesis requests for workers that turn fabrication intent, constraints, parametric templates, learning hints, and CAD references into generated design candidates.
/// Service: dd-fabrication-server
pub const FABRICATION_DESIGN_SYNTHESIS_REQUESTS_SUBJECT: &str = "dd.remote.fabrication.design.synthesis.requests";
pub const FABRICATION_DESIGN_SYNTHESIS_REQUESTS_QUEUE_GROUP: &str = "dd-fabrication-design-synthesizers";

/// Design synthesis results carrying generated design candidates, parametric source artifacts, manufacturability evidence, blockers, and review metadata.
/// Service: dd-fabrication-server
pub const FABRICATION_DESIGN_SYNTHESIS_RESULTS_SUBJECT: &str = "dd.remote.fabrication.design.synthesis.results";

/// Fabrication execution telemetry requests for live printer, mill, lathe, router, sheet-cutting, assembly, inspection, and postprocess run result reviewers.
/// Service: dd-fabrication-server
pub const FABRICATION_EXECUTION_TELEMETRY_REQUESTS_SUBJECT: &str = "dd.remote.fabrication.execution.telemetry.requests";
pub const FABRICATION_EXECUTION_TELEMETRY_REQUESTS_QUEUE_GROUP: &str = "dd-fabrication-execution-reviewers";

/// Fabrication execution telemetry results carrying observed run state, machine stops, operator interventions, split/combine decisions, artifacts, and learning labels.
/// Service: dd-fabrication-server
pub const FABRICATION_EXECUTION_TELEMETRY_RESULTS_SUBJECT: &str = "dd.remote.fabrication.execution.telemetry.results";

/// Instruction generation requests for slicer, CAM, postprocess, setup-sheet, and machine-code workers.
/// Service: dd-fabrication-server
pub const FABRICATION_INSTRUCTION_GENERATION_REQUESTS_SUBJECT: &str = "dd.remote.fabrication.instructions.generation.requests";
pub const FABRICATION_INSTRUCTION_GENERATION_REQUESTS_QUEUE_GROUP: &str = "dd-fabrication-instruction-generators";

/// Instruction generation results carrying machine code, setup sheets, simulation evidence, blockers, and release metadata.
/// Service: dd-fabrication-server
pub const FABRICATION_INSTRUCTION_GENERATION_RESULTS_SUBJECT: &str = "dd.remote.fabrication.instructions.generation.results";

/// Imported fabrication instruction review requests for existing G-code, NC programs, slicer files, setup sheets, sheet-cutting jobs, and operator instructions.
/// Service: dd-fabrication-server
pub const FABRICATION_INSTRUCTION_REVIEW_REQUESTS_SUBJECT: &str = "dd.remote.fabrication.instructions.review.requests";
pub const FABRICATION_INSTRUCTION_REVIEW_REQUESTS_QUEUE_GROUP: &str = "dd-fabrication-instruction-reviewers";

/// Imported fabrication instruction review results carrying validation findings, improvement drafts, machine-failure boundaries, and release blockers.
/// Service: dd-fabrication-server
pub const FABRICATION_INSTRUCTION_REVIEW_RESULTS_SUBJECT: &str = "dd.remote.fabrication.instructions.review.results";

/// Instruction simulation and verification requests for generated or imported printer jobs, G-code, NC programs, lathe cycles, sheet-cutting files, and setup evidence.
/// Service: dd-fabrication-server
pub const FABRICATION_INSTRUCTION_SIMULATION_REQUESTS_SUBJECT: &str = "dd.remote.fabrication.instructions.simulation.requests";
pub const FABRICATION_INSTRUCTION_SIMULATION_REQUESTS_QUEUE_GROUP: &str = "dd-fabrication-instruction-simulators";

/// Instruction simulation and verification results carrying machine-envelope checks, toolpath/process findings, failure boundaries, and release blockers.
/// Service: dd-fabrication-server
pub const FABRICATION_INSTRUCTION_SIMULATION_RESULTS_SUBJECT: &str = "dd.remote.fabrication.instructions.simulation.results";

/// Fabrication outcome learning requests for MDP, POMDP, neural-policy, reward, replay, and failure-boundary updaters.
/// Service: dd-fabrication-server
pub const FABRICATION_LEARNING_OUTCOME_REQUESTS_SUBJECT: &str = "dd.remote.fabrication.learning.outcomes.requests";
pub const FABRICATION_LEARNING_OUTCOME_REQUESTS_QUEUE_GROUP: &str = "dd-fabrication-learning-updaters";

/// Fabrication outcome learning results carrying accepted model updates, replay labels, reward summaries, and retained failure boundaries.
/// Service: dd-fabrication-server
pub const FABRICATION_LEARNING_OUTCOME_RESULTS_SUBJECT: &str = "dd.remote.fabrication.learning.outcomes.results";

/// Machine capability, calibration, fixture, tool, material, and maintenance profile requests for printers, mills, lathes, routers, sheet cutters, postprocess stations, and inspection equipment.
/// Service: dd-fabrication-server
pub const FABRICATION_MACHINE_PROFILE_REQUESTS_SUBJECT: &str = "dd.remote.fabrication.machine.profiles.requests";
pub const FABRICATION_MACHINE_PROFILE_REQUESTS_QUEUE_GROUP: &str = "dd-fabrication-machine-profilers";

/// Machine profile results carrying capability snapshots, calibration state, tool and fixture readiness, material/process state, maintenance blockers, and release evidence.
/// Service: dd-fabrication-server
pub const FABRICATION_MACHINE_PROFILE_RESULTS_SUBJECT: &str = "dd.remote.fabrication.machine.profiles.results";

/// Final fabrication release readiness requests that aggregate design, assembly, instruction, review, learning, machine, operator, and evidence state before machine-ready release.
/// Service: dd-fabrication-server
pub const FABRICATION_RELEASE_READINESS_REQUESTS_SUBJECT: &str = "dd.remote.fabrication.release.readiness.requests";
pub const FABRICATION_RELEASE_READINESS_REQUESTS_QUEUE_GROUP: &str = "dd-fabrication-release-gates";

/// Final fabrication release readiness results carrying release decisions, required human interventions, retained blockers, and machine-ready manifests.
/// Service: dd-fabrication-server
pub const FABRICATION_RELEASE_READINESS_RESULTS_SUBJECT: &str = "dd.remote.fabrication.release.readiness.results";

/// Fabrication planning requests. Default for FABRICATION_REQUEST_SUBJECT.
/// Service: dd-fabrication-server
pub const FABRICATION_REQUESTS_SUBJECT: &str = "dd.remote.fabrication.requests";
pub const FABRICATION_REQUESTS_QUEUE_GROUP: &str = "dd-fabrication-server";

/// Fabrication planning and validation results. Default for FABRICATION_RESULT_SUBJECT.
/// Service: dd-fabrication-server
pub const FABRICATION_RESULTS_SUBJECT: &str = "dd.remote.fabrication.results";

/// Inbound function-approximation requests (a dataset plus an optional method and config) consumed by the approximator. Subscribed with the dd-func-approx-rs queue group so requests load-balance across replicas. Default for FUNC_APPROX_FIT_SUBJECT.
/// Service: dd-func-approx-rs
pub const FUNC_APPROX_FIT_REQUESTS_SUBJECT: &str = "dd.remote.funcapprox.fit.requests";
pub const FUNC_APPROX_FIT_REQUESTS_QUEUE_GROUP: &str = "dd-func-approx-rs";

/// Discovered models emitted by the approximator: an analytic equation with its accuracy/complexity Pareto front and symbolic derivatives, or an MLP / polynomial specification, with train/validation metrics. Carries a funcapprox.fit.v1 envelope. Default for FUNC_APPROX_RESULT_SUBJECT.
/// Service: dd-func-approx-rs
pub const FUNC_APPROX_FIT_RESULTS_SUBJECT: &str = "dd.remote.funcapprox.fit.results";

/// Coalesced fan-out of known_git_repos row changes derived from the WAL/CDC stream. Published by dd-remote-rest-api so downstream services (lambda runner, build pipeline) react to git-repo metadata edits without polling.
/// Service: shared
pub const GIT_REPOS_CHANGES_SUBJECT: &str = "dd.remote.git-repos.changes";

/// Inbound GPU job-scheduling requests (a fleet of GPUs plus jobs needing VRAM and an estimated duration) consumed by the scheduler. Subscribed with the dd-gpu-rs queue group so requests load-balance across replicas. Default for GPU_JOB_SUBJECT.
/// Service: dd-gpu-rs
pub const GPU_JOB_REQUESTS_SUBJECT: &str = "dd.remote.gpu.jobs.requests";
pub const GPU_JOB_REQUESTS_QUEUE_GROUP: &str = "dd-gpu-rs";

/// Computed GPU placements (per-job gpu assignment, start/finish times, makespan, per-GPU memory utilisation, rejected jobs) emitted by the scheduler. Carries a gpu.schedule.v1 envelope. Default for GPU_RESULT_SUBJECT.
/// Service: dd-gpu-rs
pub const GPU_JOB_RESULTS_SUBJECT: &str = "dd.remote.gpu.jobs.results";

/// Inbound knowledge-graph build requests accepted over NATS. Payloads mirror the HTTP /graph/upsert and /graph/extract contracts.
/// Service: dd-knowledge-graph-builder
pub const KNOWLEDGE_GRAPH_BUILD_REQUESTS_SUBJECT: &str = "dd.remote.knowledge_graph.build.requests";
pub const KNOWLEDGE_GRAPH_BUILD_REQUESTS_QUEUE_GROUP: &str = "dd-knowledge-graph-builder";

/// Spark/Airflow graph-analytics pipeline job intents (PageRank, community detection, embeddings) generated from the constructed knowledge graph.
/// Service: dd-knowledge-graph-builder
pub const KNOWLEDGE_GRAPH_PIPELINE_JOBS_SUBJECT: &str = "dd.remote.knowledge_graph.pipeline.jobs";

/// Query, path, and centrality/analysis results from knowledge-graph runs.
/// Service: dd-knowledge-graph-builder
pub const KNOWLEDGE_GRAPH_RESULTS_SUBJECT: &str = "dd.remote.knowledge_graph.results";

/// Graph mutation events emitted after nodes and edges are upserted or extracted into the graph store. Consumers should treat this as an incremental change feed.
/// Service: dd-knowledge-graph-builder
pub const KNOWLEDGE_GRAPH_UPDATES_SUBJECT: &str = "dd.remote.knowledge_graph.updates";

/// Terminal event emitted when a durable asynchronous lambda invocation is canceled.
/// Service: dd-gleam-lambda-runner
pub const LAMBDA_ASYNC_CANCELED_SUBJECT: &str = "dd.remote.lambda.async.canceled";

/// Dead-letter event emitted after a durable asynchronous lambda invocation exhausts its retry policy.
/// Service: dd-gleam-lambda-runner
pub const LAMBDA_ASYNC_DLQ_SUBJECT: &str = "dd.remote.lambda.async.dlq";

/// Retryable failure event emitted for a durable asynchronous lambda invocation.
/// Service: dd-gleam-lambda-runner
pub const LAMBDA_ASYNC_FAILURE_SUBJECT: &str = "dd.remote.lambda.async.failure";

/// Terminal event emitted when a durable asynchronous lambda invocation succeeds.
/// Service: dd-gleam-lambda-runner
pub const LAMBDA_ASYNC_SUCCESS_SUBJECT: &str = "dd.remote.lambda.async.success";

/// Functions metadata broadcast subject. Default for NATS_LAMBDA_FUNCTIONS_SUBJECT.
/// Service: dd-gleam-lambda-runner
pub const LAMBDAS_FUNCTIONS_SUBJECT: &str = "dd.remote.lambdas.functions";

/// Results fan-out from lambda runs. Default for NATS_LAMBDA_RESULT_SUBJECT.
/// Service: dd-gleam-lambda-runner
pub const LAMBDAS_RESULTS_SUBJECT: &str = "dd.remote.lambdas.results";

/// Read-only operational control commands addressed to MCP servers, e.g. {"command":"ping"} for a liveness echo onto McpToolEvents. Broadcast with no queue group so every MCP replica receives each command. Commands must never mutate cluster state; the MCP service account only has list/read RBAC.
/// Service: dd-gleam-mcp-server
pub const MCP_CONTROL_SUBJECT: &str = "dd.remote.mcp.control";

/// Tool-call audit and lifecycle telemetry emitted by MCP servers: one event per tools/call (tool name, ok/error flag, duration) plus a lifecycle event on boot. Fan-out telemetry consumed by observability/log shippers. Payloads carry a dd.mcp_event.v1 envelope and must not contain secrets or raw cluster data.
/// Service: dd-gleam-mcp-server
pub const MCP_TOOL_EVENTS_SUBJECT: &str = "dd.remote.mcp.tool.events";

/// MDP/POMDP optimization job requests. Default for MDP_OPTIMIZE_SUBJECT.
/// Service: dd-ai-ml-pipeline
pub const MDP_OPTIMIZE_SUBJECT: &str = "dd.remote.mdp.optimize";
pub const MDP_OPTIMIZE_QUEUE_GROUP: &str = "dd-mdp-optimizer";

/// MDP optimization results published by the optimizer. Default for MDP_RESULT_SUBJECT.
/// Service: dd-ai-ml-pipeline
pub const MDP_RESULTS_SUBJECT: &str = "dd.remote.mdp.results";

/// Control-plane commands for distributed in-house MIP solves, including solve cancellation and revision invalidation.
/// Service: dd-ai-ml-pipeline
pub const MIP_SOLVER_CONTROL_SUBJECT: &str = "dd.remote.mip_solver.control";
pub const MIP_SOLVER_CONTROL_STREAM: &str = "DD_REMOTE_MIP_SOLVER";

/// Lifecycle and progress events from distributed in-house LP/MIP/IP solver nodes.
/// Service: dd-ai-ml-pipeline
pub const MIP_SOLVER_EVENTS_SUBJECT: &str = "dd.remote.mip_solver.events";
pub const MIP_SOLVER_EVENTS_STREAM: &str = "DD_REMOTE_MIP_SOLVER";

/// Distributed in-house LP/MIP/IP subproblem jobs. Masters publish branch-and-bound relaxation/subtree work; slave pods consume through the worker queue group.
/// Service: dd-ai-ml-pipeline
pub const MIP_SOLVER_JOBS_SUBJECT: &str = "dd.remote.mip_solver.jobs";
pub const MIP_SOLVER_JOBS_QUEUE_GROUP: &str = "dd-in-house-mip-solver-node-workers";
pub const MIP_SOLVER_JOBS_STREAM: &str = "DD_REMOTE_MIP_SOLVER";

/// Distributed in-house LP/MIP/IP subproblem results. Slaves publish solved LP relaxation/subtree summaries and masters aggregate them by solveId.
/// Service: dd-ai-ml-pipeline
pub const MIP_SOLVER_RESULTS_SUBJECT: &str = "dd.remote.mip_solver.results";
pub const MIP_SOLVER_RESULTS_STREAM: &str = "DD_REMOTE_MIP_SOLVER";

/// Dead-letter target for ai-ml-pipeline messages that failed validation/processing. Default for ML_DEAD_LETTER_SUBJECT.
/// Service: dd-ai-ml-pipeline
pub const ML_DEAD_LETTER_SUBJECT: &str = "dd.remote.ml.deadletter";

/// Derived ML features (z-scores, EWMA baselines) published by ai-ml-pipeline. Default for ML_FEATURE_SUBJECT.
/// Service: dd-ai-ml-pipeline
pub const ML_FEATURES_SUBJECT: &str = "dd.remote.ml.features";

/// Inbound Monte Carlo simulation requests consumed by the monte-carlo server. Subscribed with the dd-monte-carlo-server queue group so requests load-balance across replicas. Default for MONTE_CARLO_SIMULATE_SUBJECT.
/// Service: dd-monte-carlo-server
pub const MONTE_CARLO_SIMULATE_REQUESTS_SUBJECT: &str = "dd.remote.montecarlo.simulate.requests";
pub const MONTE_CARLO_SIMULATE_REQUESTS_QUEUE_GROUP: &str = "dd-monte-carlo-server";

/// Monte Carlo estimates (point estimate, standard error, confidence interval, per-experiment summary) emitted by the monte-carlo server. Carries a montecarlo.simulate.v1 envelope. Default for MONTE_CARLO_RESULT_SUBJECT.
/// Service: dd-monte-carlo-server
pub const MONTE_CARLO_SIMULATE_RESULTS_SUBJECT: &str = "dd.remote.montecarlo.simulate.results";

/// Song-generation requests consumed by the music server. Subscribed with the dd-music-rs queue group so requests load-balance across replicas. The consumer is opt-in (MUSIC_NATS_GENERATION_ENABLED) because each request drives expensive synthesis; publish rights to this subject are a privileged capability.
/// Service: dd-music-rs
pub const MUSIC_GENERATION_REQUESTS_SUBJECT: &str = "dd.remote.music.generation.requests";
pub const MUSIC_GENERATION_REQUESTS_QUEUE_GROUP: &str = "dd-music-rs";

/// Generation outcomes (counts of published / discarded-below-listenability, attempts, and published song ids) emitted after a generation pass — whether triggered by the scheduled sweep, the HTTP endpoint, or a NATS request.
/// Service: dd-music-rs
pub const MUSIC_GENERATION_RESULTS_SUBJECT: &str = "dd.remote.music.generation.results";

/// Fan-out event emitted after a newly generated song is published to storage. Carries song metadata and the public audio URL (never audio bytes). Broadcast with no queue group so every interested consumer receives it.
/// Service: dd-music-rs
pub const MUSIC_SONGS_PUBLISHED_SUBJECT: &str = "dd.remote.music.songs.published";

/// Fan-out of anonymous up/down votes for downstream analytics. Carries song id, direction, and the resulting tallies (no visitor hashes).
/// Service: dd-music-rs
pub const MUSIC_VOTES_EVENTS_SUBJECT: &str = "dd.remote.music.votes.events";

/// OCR requests consumed by dd-ocr-rs replicas.
/// Service: dd-ocr-rs
pub const OCR_REQUESTS_SUBJECT: &str = "dd.remote.ocr.requests";
pub const OCR_REQUESTS_QUEUE_GROUP: &str = "dd-ocr-rs";

/// OCR results published after a request completes or fails.
/// Service: dd-ocr-rs
pub const OCR_RESULTS_SUBJECT: &str = "dd.remote.ocr.results";

/// Wakeup signal published whenever a new task is enqueued for a thread, so the orchestrator can prepare/scale the matching worker deployment without polling.
/// Service: dd-remote-rest-api
pub const ORCHESTRATOR_WAKEUP_SUBJECT: &str = "dd.remote.orchestrator.wakeup";
pub const ORCHESTRATOR_WAKEUP_STREAM: &str = "DD_REMOTE_CONTROL";

/// Trend, correlation, grant-match, graph-data, model, and white-paper evidence results from public-data analysis runs.
/// Service: dd-public-data-server
pub const PUBLIC_DATA_ANALYSIS_RESULTS_SUBJECT: &str = "dd.remote.public_data.analysis.results";

/// Dead-letter subject for public-data ingest requests that exhaust JetStream delivery attempts.
/// Service: dd-public-data-server
pub const PUBLIC_DATA_INGEST_DEAD_LETTER_SUBJECT: &str = "dd.remote.public_data.ingest.deadletter";

/// Inbound public-data ingestion requests accepted over NATS. Payloads mirror the HTTP /ingest and /scrape contracts.
/// Service: dd-public-data-server
pub const PUBLIC_DATA_INGEST_REQUESTS_SUBJECT: &str = "dd.remote.public_data.ingest.requests";
pub const PUBLIC_DATA_INGEST_REQUESTS_QUEUE_GROUP: &str = "dd-public-data-server";

/// Results emitted after public-data records, scrape results, or webhook receipts are normalized into the service ledger.
/// Service: dd-public-data-server
pub const PUBLIC_DATA_INGEST_RESULTS_SUBJECT: &str = "dd.remote.public_data.ingest.results";

/// Spark/Airflow pipeline job intents generated from ingested public datasets, grants, trend/correlation analysis, and white-paper evidence briefs.
/// Service: dd-public-data-server
pub const PUBLIC_DATA_PIPELINE_JOBS_SUBJECT: &str = "dd.remote.public_data.pipeline.jobs";

/// Raw-but-redacted webhook receipt events from public/primary data providers. Consumers should use this as an audit/event source, not the canonical dataset store.
/// Service: dd-public-data-server
pub const PUBLIC_DATA_WEBHOOK_EVENTS_SUBJECT: &str = "dd.remote.public_data.webhooks.events";

/// Opt-in inbound command that wakes one tenant ledger sync loop; replicas load-balance through the Quaestor queue group.
/// Service: dd-quaestor-web-server
pub const QUAESTOR_LEDGER_COMMANDS_SUBJECT: &str = "dd.remote.quaestor.ledger.commands.sync";
pub const QUAESTOR_LEDGER_COMMANDS_QUEUE_GROUP: &str = "dd-quaestor-web-server";

/// Best-effort redacted event emitted when a ledger account relationship is observed.
/// Service: dd-quaestor-web-server
pub const QUAESTOR_LEDGER_OBSERVE_SUBJECT: &str = "dd.remote.quaestor.ledger.observe";

/// Best-effort redacted event emitted when an append-only per-account balance proof is produced.
/// Service: dd-quaestor-web-server
pub const QUAESTOR_LEDGER_PROVE_SUBJECT: &str = "dd.remote.quaestor.ledger.prove";

/// Best-effort redacted event emitted when a journal line is recorded; memo content is represented only by its SHA-256 digest.
/// Service: dd-quaestor-web-server
pub const QUAESTOR_LEDGER_RECORD_SUBJECT: &str = "dd.remote.quaestor.ledger.record";

/// Inbound quantum-simulation requests (a mode plus a circuit, oracle, graph, or Hamiltonian and optional config) consumed by the simulator. Subscribed with the dd-quantum-compute-rs queue group so requests load-balance across replicas. Default for QUANTUM_SOLVE_SUBJECT.
/// Service: dd-quantum-compute-rs
pub const QUANTUM_SOLVE_REQUESTS_SUBJECT: &str = "dd.remote.quantum.solve.requests";
pub const QUANTUM_SOLVE_REQUESTS_QUEUE_GROUP: &str = "dd-quantum-compute-rs";

/// Simulation results emitted by the quantum simulator: a measurement distribution and final-state amplitudes plus the per-mode answer (Grover's amplified item and success probability, QAOA's best bitstring and cut value, or the VQE ground-state energy estimate). Carries a quantum.solve.v1 envelope. Default for QUANTUM_SOLVE_RESULT_SUBJECT.
/// Service: dd-quantum-compute-rs
pub const QUANTUM_SOLVE_RESULTS_SUBJECT: &str = "dd.remote.quantum.solve.results";

/// Fan-out of per-step consensus state transitions (role changes, elections won/lost, entries appended/committed, dropped/partitioned messages) for live observation and chaos-test assertions. Broadcast with no queue group. Default for RAFT_EVENT_SUBJECT.
/// Service: dd-raft-consensus
pub const RAFT_CONSENSUS_EVENTS_SUBJECT: &str = "dd.remote.raft.consensus.events";

/// Consensus outcomes (committed log, term/leader history, election counts, divergence detection) emitted after a simulated run. Carries a raft.consensus.v1 envelope. Default for RAFT_RESULT_SUBJECT.
/// Service: dd-raft-consensus
pub const RAFT_CONSENSUS_RESULTS_SUBJECT: &str = "dd.remote.raft.consensus.results";

/// Inbound Raft scenario/propose requests (cluster size, command stream, chaos parameters) consumed by the raft-consensus simulator. Subscribed with the dd-raft-consensus queue group so requests load-balance across replicas. Default for RAFT_PROPOSE_SUBJECT.
/// Service: dd-raft-consensus
pub const RAFT_PROPOSE_REQUESTS_SUBJECT: &str = "dd.remote.raft.propose.requests";
pub const RAFT_PROPOSE_REQUESTS_QUEUE_GROUP: &str = "dd-raft-consensus";

/// Inbound TSP/VRP route-optimization requests consumed by the route optimizer. Subscribed with the dd-route-optimizer queue group so requests load-balance across replicas. Default for ROUTE_OPTIMIZE_SUBJECT.
/// Service: dd-route-optimizer
pub const ROUTE_OPTIMIZE_REQUESTS_SUBJECT: &str = "dd.remote.route.optimize.requests";
pub const ROUTE_OPTIMIZE_REQUESTS_QUEUE_GROUP: &str = "dd-route-optimizer";

/// Optimized routes (ordered stops per vehicle, total distance, arrival times, time-window violations) emitted by the route optimizer. Carries a route.optimize.v1 envelope. Default for ROUTE_RESULT_SUBJECT.
/// Service: dd-route-optimizer
pub const ROUTE_OPTIMIZE_RESULTS_SUBJECT: &str = "dd.remote.route.optimize.results";

/// Incumbent-improvement and lifecycle events. The master emits a new event every time the best-known tour improves; the canvas dashboard renders these live.
/// Service: dd-routing-server
pub const ROUTING_EVENTS_SUBJECT: &str = "dd.remote.routing.events";
pub const ROUTING_EVENTS_STREAM: &str = "DD_REMOTE_ROUTING";

/// Per-worker routing jobs. The master publishes one construction+local-search restart (a seeded nearest-neighbour or random start) for a problem; worker pods consume through the workers queue group and return an improved tour.
/// Service: dd-routing-server
pub const ROUTING_JOBS_SUBJECT: &str = "dd.remote.routing.jobs";
pub const ROUTING_JOBS_QUEUE_GROUP: &str = "dd-routing-server-workers";
pub const ROUTING_JOBS_STREAM: &str = "DD_REMOTE_ROUTING";

/// Per-worker routing results. Workers publish a completed tour (or per-vehicle routes) with its total distance; the master keeps the global incumbent by solveId.
/// Service: dd-routing-server
pub const ROUTING_RESULTS_SUBJECT: &str = "dd.remote.routing.results";
pub const ROUTING_RESULTS_STREAM: &str = "DD_REMOTE_ROUTING";

/// Critical operational event bus for compact alert-worthy runtime failures. JetStream-backed by DD_REMOTE_CRITICAL_EVENTS so dd-remote-queue-consumer can log/alert without losing events during restarts. Payloads should carry a dd.log.v1-compatible envelope and must not contain secrets.
/// Service: shared
pub const RUNTIME_CRITICAL_EVENTS_SUBJECT: &str = "dd.remote.events.critical";
pub const RUNTIME_CRITICAL_EVENTS_QUEUE_GROUP: &str = "dd-runtime-critical-events";
pub const RUNTIME_CRITICAL_EVENTS_STREAM: &str = "DD_REMOTE_CRITICAL_EVENTS";

/// Generic runtime event bus. Every deployment publishes lifecycle, error, telemetry-style events here. The default for NATS_EVENT_SUBJECT across the codebase.
/// Service: shared
pub const RUNTIME_EVENTS_SUBJECT: &str = "dd.remote.events";

/// Inbound CNF SAT / SMT solve requests consumed by the sat-smt server. Subscribed with the dd-sat-smt-server queue group so requests load-balance across replicas. Default for SAT_SOLVE_SUBJECT.
/// Service: dd-sat-smt-server
pub const SAT_SOLVE_REQUESTS_SUBJECT: &str = "dd.remote.sat.solve.requests";
pub const SAT_SOLVE_REQUESTS_QUEUE_GROUP: &str = "dd-sat-smt-server";

/// Solve outcomes (sat/unsat/unknown with a model when satisfiable) emitted by the sat-smt server. Carries a sat.solve.v1 envelope. Default for SAT_RESULT_SUBJECT.
/// Service: dd-sat-smt-server
pub const SAT_SOLVE_RESULTS_SUBJECT: &str = "dd.remote.sat.solve.results";

/// Inbound constraint-scheduling requests (job-shop, rostering, timetabling) consumed by the scheduler. Subscribed with the dd-constraint-scheduler queue group so requests load-balance across replicas. Default for SCHEDULER_SCHEDULE_SUBJECT.
/// Service: dd-constraint-scheduler
pub const SCHEDULER_SCHEDULE_REQUESTS_SUBJECT: &str = "dd.remote.scheduler.schedule.requests";
pub const SCHEDULER_SCHEDULE_REQUESTS_QUEUE_GROUP: &str = "dd-constraint-scheduler";

/// Computed schedules (task start times, makespan, machine assignments, violations) emitted by the scheduler. Carries a scheduler.schedule.v1 envelope. Default for SCHEDULER_RESULT_SUBJECT.
/// Service: dd-constraint-scheduler
pub const SCHEDULER_SCHEDULE_RESULTS_SUBJECT: &str = "dd.remote.scheduler.schedule.results";

/// MDP telemetry requests emitted by ai-ml-pipeline and consumed by the MDP optimizer. Default for ML_MDP_TELEMETRY_SUBJECT / MDP_TELEMETRY_SUBJECT.
/// Service: dd-ai-ml-pipeline
pub const TELEMETRY_MDP_SUBJECT: &str = "dd.remote.telemetry.mdp";

/// Raw runtime telemetry samples ingested by the ai-ml-pipeline. Default for ML_RAW_TELEMETRY_SUBJECT.
/// Service: dd-ai-ml-pipeline
pub const TELEMETRY_RAW_SUBJECT: &str = "dd.remote.telemetry.raw";
pub const TELEMETRY_RAW_QUEUE_GROUP: &str = "dd-ai-ml-pipeline";

/// Redacted terminal task failures emitted after the queue consumer exhausts JetStream redelivery. Kept on a separate limits-retention stream so poison-message evidence is durable without affecting the WorkQueue consumer lag used by KEDA.
/// Service: dd-remote-rest-api
pub const THREAD_TASKS_DEAD_LETTER_SUBJECT: &str = "dd.remote.thread.tasks.deadletter";
pub const THREAD_TASKS_DEAD_LETTER_STREAM: &str = "DD_REMOTE_TASKS_DLQ";

/// Risk-gated buy/sell/hold decisions emitted by the trading server. Default for TRADING_DECISION_SUBJECT.
/// Service: dd-trading-server
pub const TRADING_DECISIONS_SUBJECT: &str = "dd.remote.trading.decisions";

/// Concrete order intents derived from decisions. Default for TRADING_ORDER_INTENT_SUBJECT.
/// Service: dd-trading-server
pub const TRADING_ORDER_INTENTS_SUBJECT: &str = "dd.remote.trading.order_intents";

/// Inbound market signals consumed by the trading server. Default for TRADING_SIGNAL_SUBJECT.
/// Service: dd-trading-server
pub const TRADING_SIGNALS_SUBJECT: &str = "dd.remote.trading.signals";
pub const TRADING_SIGNALS_QUEUE_GROUP: &str = "dd-trading-server";

/// WebSocket event bridge used by gleamlang-server and gleamlang-ws-server. Defaults to NATS_PUBLISH_SUBJECT='dd.remote.websocket.events'.
/// Service: shared
pub const WEBSOCKET_EVENTS_SUBJECT: &str = "dd.remote.websocket.events";

/// Run lifecycle and step events fanned out by the engine (run started/step succeeded/step failed/run completed/run failed/run canceled). Fan-out telemetry for visibility and downstream subscribers; not the durable source of truth. Default for NATS_WORKFLOW_EVENT_SUBJECT.
/// Service: dd-gleam-lambda-runner
pub const WORKFLOWS_EVENTS_SUBJECT: &str = "dd.remote.workflows.events";

/// Start a new workflow run. Request/reply: payload is {"definitionId"|"definitionSlug", "input"?, "idempotencyKey"?}; the reply carries the created run id and status. Consumed by the runner's queue group so exactly one replica creates the run. Default for NATS_WORKFLOW_START_SUBJECT='dd.remote.workflows.start'.
/// Service: dd-gleam-lambda-runner
pub const WORKFLOWS_START_SUBJECT: &str = "dd.remote.workflows.start";
pub const WORKFLOWS_START_QUEUE_GROUP: &str = "dd-gleam-workflow-engine";

// ---------- Parameterized subjects ----------

/// Per-job lifecycle and progress events emitted by the isolated browser worker.
/// Service: dd-browser-job-runner
pub const BROWSER_JOB_EVENTS_PATTERN: &str = "dd.remote.browser_jobs.{job_id}.events";
pub const BROWSER_JOB_EVENTS_WILDCARD: &str = "dd.remote.browser_jobs.*.events";
pub fn browser_job_events_subject(job_id: &str) -> String {
    format!("dd.remote.browser_jobs.{}.events", job_id)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct BrowserJobEventsSubjectParts {
    pub job_id: String,
}

pub fn parse_browser_job_events_subject(subject: &str) -> Option<BrowserJobEventsSubjectParts> {
    let pattern_tokens: &[&str] = &["dd", "remote", "browser_jobs", "{job_id}", "events"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut job_id: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "job_id" => { job_id = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(BrowserJobEventsSubjectParts {
        job_id: job_id?,
    })
}

/// Per-job terminal result emitted by the isolated browser worker.
/// Service: dd-browser-job-runner
pub const BROWSER_JOB_RESULT_PATTERN: &str = "dd.remote.browser_jobs.{job_id}.result";
pub const BROWSER_JOB_RESULT_WILDCARD: &str = "dd.remote.browser_jobs.*.result";
pub fn browser_job_result_subject(job_id: &str) -> String {
    format!("dd.remote.browser_jobs.{}.result", job_id)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct BrowserJobResultSubjectParts {
    pub job_id: String,
}

pub fn parse_browser_job_result_subject(subject: &str) -> Option<BrowserJobResultSubjectParts> {
    let pattern_tokens: &[&str] = &["dd", "remote", "browser_jobs", "{job_id}", "result"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut job_id: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "job_id" => { job_id = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(BrowserJobResultSubjectParts {
        job_id: job_id?,
    })
}

/// Per-row change emitted by wal-gateway. Subject pattern is '<prefix>.<schema>.<table>.<op>'. The default prefix is 'cdc' and the default stream name is 'CDC'. Consumers usually subscribe to the prefix tail wildcard ('cdc.>').
/// Service: dd-wal-gateway
pub const CDC_ROW_CHANGE_PATTERN: &str = "{prefix}.{schema}.{table}.{op}";
pub const CDC_ROW_CHANGE_WILDCARD: &str = "{prefix}.>";
pub const CDC_ROW_CHANGE_STREAM: &str = "CDC";
pub fn cdc_row_change_subject(prefix: &str, schema: &str, table: &str, op: &str) -> String {
    format!("{}.{}.{}.{}", prefix, schema, table, op)
}

pub fn format_cdc_row_change_wildcard(prefix: &str) -> String {
    format!("{}.>", prefix)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct CdcRowChangeSubjectParts {
    pub prefix: String,
    pub schema: String,
    pub table: String,
    pub op: String,
}

pub fn parse_cdc_row_change_subject(subject: &str) -> Option<CdcRowChangeSubjectParts> {
    let pattern_tokens: &[&str] = &["{prefix}", "{schema}", "{table}", "{op}"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut prefix: Option<String> = None;
    let mut schema: Option<String> = None;
    let mut table: Option<String> = None;
    let mut op: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "prefix" => { prefix = Some(subject_tokens[si].to_string()); }
                "schema" => { schema = Some(subject_tokens[si].to_string()); }
                "table" => { table = Some(subject_tokens[si].to_string()); }
                "op" => { op = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(CdcRowChangeSubjectParts {
        prefix: prefix?,
        schema: schema?,
        table: table?,
        op: op?,
    })
}

/// Per-table JetStream filter subject ('<prefix>.<schema>.<table>.>') used by CDC consumers (e.g. dd-remote-rest-api) to subscribe to every op for one Postgres table. Not a publish target; producers publish per-row via CdcRowChange.
/// Service: dd-wal-gateway
pub const CDC_TABLE_FILTER_PATTERN: &str = "{prefix}.{schema}.{table}.>";
pub const CDC_TABLE_FILTER_WILDCARD: &str = "{prefix}.>";
pub const CDC_TABLE_FILTER_STREAM: &str = "CDC";
pub fn cdc_table_filter_subject(prefix: &str, schema: &str, table: &str) -> String {
    format!("{}.{}.{}.>", prefix, schema, table)
}

pub fn format_cdc_table_filter_wildcard(prefix: &str) -> String {
    format!("{}.>", prefix)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct CdcTableFilterSubjectParts {
    pub prefix: String,
    pub schema: String,
    pub table: String,
}

pub fn parse_cdc_table_filter_subject(subject: &str) -> Option<CdcTableFilterSubjectParts> {
    let pattern_tokens: &[&str] = &["{prefix}", "{schema}", "{table}", ">"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut prefix: Option<String> = None;
    let mut schema: Option<String> = None;
    let mut table: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "prefix" => { prefix = Some(subject_tokens[si].to_string()); }
                "schema" => { schema = Some(subject_tokens[si].to_string()); }
                "table" => { table = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(CdcTableFilterSubjectParts {
        prefix: prefix?,
        schema: schema?,
        table: table?,
    })
}

/// Structured CloudEvents input routed to active lambda bindings. Consumers subscribe to the terminal wildcard; producers publish beneath dd.remote.cloudevents with at least one event token.
/// Service: dd-gleam-lambda-runner
pub const CLOUD_EVENTS_INPUT_PATTERN: &str = "dd.remote.cloudevents.{event_type}";
pub const CLOUD_EVENTS_INPUT_WILDCARD: &str = "dd.remote.cloudevents.>";
pub const CLOUD_EVENTS_INPUT_QUEUE_GROUP: &str = "dd-gleam-lambda-runner-events";
pub fn cloud_events_input_subject(event_type: &str) -> String {
    format!("dd.remote.cloudevents.{}", event_type)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct CloudEventsInputSubjectParts {
    pub event_type: String,
}

pub fn parse_cloud_events_input_subject(subject: &str) -> Option<CloudEventsInputSubjectParts> {
    let pattern_tokens: &[&str] = &["dd", "remote", "cloudevents", "{event_type}"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut event_type: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "event_type" => { event_type = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(CloudEventsInputSubjectParts {
        event_type: event_type?,
    })
}

/// Per-pool event subject (built from pool slug at runtime).
/// Service: dd-container-pool
pub const CONTAINER_POOL_EVENTS_PATTERN: &str = "dd.remote.container_pool.{pool_slug}.events";
pub const CONTAINER_POOL_EVENTS_WILDCARD: &str = "dd.remote.container_pool.*.events";
pub fn container_pool_events_subject(pool_slug: &str) -> String {
    format!("dd.remote.container_pool.{}.events", pool_slug)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ContainerPoolEventsSubjectParts {
    pub pool_slug: String,
}

pub fn parse_container_pool_events_subject(subject: &str) -> Option<ContainerPoolEventsSubjectParts> {
    let pattern_tokens: &[&str] = &["dd", "remote", "container_pool", "{pool_slug}", "events"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut pool_slug: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "pool_slug" => { pool_slug = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(ContainerPoolEventsSubjectParts {
        pool_slug: pool_slug?,
    })
}

/// Per-pool heartbeat subject (built from pool slug at runtime).
/// Service: dd-container-pool
pub const CONTAINER_POOL_HEARTBEATS_PATTERN: &str = "dd.remote.container_pool.{pool_slug}.heartbeats";
pub const CONTAINER_POOL_HEARTBEATS_WILDCARD: &str = "dd.remote.container_pool.*.heartbeats";
pub fn container_pool_heartbeats_subject(pool_slug: &str) -> String {
    format!("dd.remote.container_pool.{}.heartbeats", pool_slug)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ContainerPoolHeartbeatsSubjectParts {
    pub pool_slug: String,
}

pub fn parse_container_pool_heartbeats_subject(subject: &str) -> Option<ContainerPoolHeartbeatsSubjectParts> {
    let pattern_tokens: &[&str] = &["dd", "remote", "container_pool", "{pool_slug}", "heartbeats"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut pool_slug: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "pool_slug" => { pool_slug = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(ContainerPoolHeartbeatsSubjectParts {
        pool_slug: pool_slug?,
    })
}

/// Per-runtime pool request subject (e.g. nodejs, rust, golang, dart, gleamlang, erlang). Seeded by container-pool-app-config.
/// Service: dd-container-pool
pub const CONTAINER_POOL_LANGUAGE_REQUESTS_PATTERN: &str = "dd.remote.container_pool.{language}.requests";
pub const CONTAINER_POOL_LANGUAGE_REQUESTS_WILDCARD: &str = "dd.remote.container_pool.*.requests";
pub fn container_pool_language_requests_subject(language: &str) -> String {
    format!("dd.remote.container_pool.{}.requests", language)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ContainerPoolLanguageRequestsSubjectParts {
    pub language: String,
}

pub fn parse_container_pool_language_requests_subject(subject: &str) -> Option<ContainerPoolLanguageRequestsSubjectParts> {
    let pattern_tokens: &[&str] = &["dd", "remote", "container_pool", "{language}", "requests"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut language: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "language" => { language = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(ContainerPoolLanguageRequestsSubjectParts {
        language: language?,
    })
}

/// Invocation subject for a single lambda. Consumers subscribe to the wildcard using the runner's queue group; producers publish to the specific function token. Default for NATS_LAMBDA_INVOKE_SUBJECT='dd.remote.lambdas.invoke.*'.
/// Service: dd-gleam-lambda-runner
pub const LAMBDAS_INVOKE_PATTERN: &str = "dd.remote.lambdas.invoke.{function_name}";
pub const LAMBDAS_INVOKE_WILDCARD: &str = "dd.remote.lambdas.invoke.*";
pub const LAMBDAS_INVOKE_QUEUE_GROUP: &str = "dd-gleam-lambda-runner";
pub fn lambdas_invoke_subject(function_name: &str) -> String {
    format!("dd.remote.lambdas.invoke.{}", function_name)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct LambdasInvokeSubjectParts {
    pub function_name: String,
}

pub fn parse_lambdas_invoke_subject(subject: &str) -> Option<LambdasInvokeSubjectParts> {
    let pattern_tokens: &[&str] = &["dd", "remote", "lambdas", "invoke", "{function_name}"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut function_name: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "function_name" => { function_name = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(LambdasInvokeSubjectParts {
        function_name: function_name?,
    })
}

/// Broadcast a websocket frame to every connection in a conv. Consumers subscribe via the wildcard 'presence.broadcast.conv.>' so they receive all conv broadcasts and dispatch locally.
/// Service: dd-gleamlang-presence-server
pub const PRESENCE_BROADCAST_CONV_PATTERN: &str = "presence.broadcast.conv.{conv_id}";
pub const PRESENCE_BROADCAST_CONV_WILDCARD: &str = "presence.broadcast.conv.>";
pub fn presence_broadcast_conv_subject(conv_id: &str) -> String {
    format!("presence.broadcast.conv.{}", conv_id)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PresenceBroadcastConvSubjectParts {
    pub conv_id: String,
}

pub fn parse_presence_broadcast_conv_subject(subject: &str) -> Option<PresenceBroadcastConvSubjectParts> {
    let pattern_tokens: &[&str] = &["presence", "broadcast", "conv", "{conv_id}"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut conv_id: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "conv_id" => { conv_id = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(PresenceBroadcastConvSubjectParts {
        conv_id: conv_id?,
    })
}

/// Optional per-conv membership-change channel (reserved; today membership changes go through PG NOTIFY).
/// Service: dd-gleamlang-presence-server
pub const PRESENCE_MEMBER_CHANGE_CONV_PATTERN: &str = "presence.member_change.conv.{conv_id}";
pub const PRESENCE_MEMBER_CHANGE_CONV_WILDCARD: &str = "presence.member_change.conv.>";
pub fn presence_member_change_conv_subject(conv_id: &str) -> String {
    format!("presence.member_change.conv.{}", conv_id)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PresenceMemberChangeConvSubjectParts {
    pub conv_id: String,
}

pub fn parse_presence_member_change_conv_subject(subject: &str) -> Option<PresenceMemberChangeConvSubjectParts> {
    let pattern_tokens: &[&str] = &["presence", "member_change", "conv", "{conv_id}"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut conv_id: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "conv_id" => { conv_id = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(PresenceMemberChangeConvSubjectParts {
        conv_id: conv_id?,
    })
}

/// Per-thread control-plane commands (scale, evict, reload). Short-retention JetStream control stream.
/// Service: dd-remote-rest-api
pub const THREAD_CONTROL_PATTERN: &str = "dd.remote.thread.{thread_id}.control";
pub const THREAD_CONTROL_WILDCARD: &str = "dd.remote.thread.*.control";
pub const THREAD_CONTROL_STREAM: &str = "DD_REMOTE_CONTROL";
pub fn thread_control_subject(thread_id: &str) -> String {
    format!("dd.remote.thread.{}.control", thread_id)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ThreadControlSubjectParts {
    pub thread_id: String,
}

pub fn parse_thread_control_subject(subject: &str) -> Option<ThreadControlSubjectParts> {
    let pattern_tokens: &[&str] = &["dd", "remote", "thread", "{thread_id}", "control"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut thread_id: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "thread_id" => { thread_id = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(ThreadControlSubjectParts {
        thread_id: thread_id?,
    })
}

/// Per-thread worker events (progress, status, output). Longer-retention JetStream events stream.
/// Service: dd-remote-rest-api
pub const THREAD_EVENTS_PATTERN: &str = "dd.remote.thread.{thread_id}.events";
pub const THREAD_EVENTS_WILDCARD: &str = "dd.remote.thread.*.events";
pub const THREAD_EVENTS_STREAM: &str = "DD_REMOTE_EVENTS";
pub fn thread_events_subject(thread_id: &str) -> String {
    format!("dd.remote.thread.{}.events", thread_id)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ThreadEventsSubjectParts {
    pub thread_id: String,
}

pub fn parse_thread_events_subject(subject: &str) -> Option<ThreadEventsSubjectParts> {
    let pattern_tokens: &[&str] = &["dd", "remote", "thread", "{thread_id}", "events"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut thread_id: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "thread_id" => { thread_id = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(ThreadEventsSubjectParts {
        thread_id: thread_id?,
    })
}

/// Per-thread worker liveness pings.
/// Service: dd-remote-rest-api
pub const THREAD_HEARTBEAT_PATTERN: &str = "dd.remote.thread.{thread_id}.heartbeat";
pub const THREAD_HEARTBEAT_WILDCARD: &str = "dd.remote.thread.*.heartbeat";
pub const THREAD_HEARTBEAT_STREAM: &str = "DD_REMOTE_EVENTS";
pub fn thread_heartbeat_subject(thread_id: &str) -> String {
    format!("dd.remote.thread.{}.heartbeat", thread_id)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ThreadHeartbeatSubjectParts {
    pub thread_id: String,
}

pub fn parse_thread_heartbeat_subject(subject: &str) -> Option<ThreadHeartbeatSubjectParts> {
    let pattern_tokens: &[&str] = &["dd", "remote", "thread", "{thread_id}", "heartbeat"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut thread_id: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "thread_id" => { thread_id = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(ThreadHeartbeatSubjectParts {
        thread_id: thread_id?,
    })
}

/// Per-thread task queue. JetStream-backed (DD_REMOTE_TASKS) with WorkQueue retention. Producers publish per-thread and queue-consumer replicas share the durable wildcard consumer so each task has one handoff owner.
/// Service: dd-remote-rest-api
pub const THREAD_TASKS_PATTERN: &str = "dd.remote.thread.{thread_id}.tasks";
pub const THREAD_TASKS_WILDCARD: &str = "dd.remote.thread.*.tasks";
pub const THREAD_TASKS_QUEUE_GROUP: &str = "dd-remote-thread-preparer";
pub const THREAD_TASKS_STREAM: &str = "DD_REMOTE_TASKS";
pub fn thread_tasks_subject(thread_id: &str) -> String {
    format!("dd.remote.thread.{}.tasks", thread_id)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ThreadTasksSubjectParts {
    pub thread_id: String,
}

pub fn parse_thread_tasks_subject(subject: &str) -> Option<ThreadTasksSubjectParts> {
    let pattern_tokens: &[&str] = &["dd", "remote", "thread", "{thread_id}", "tasks"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut thread_id: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "thread_id" => { thread_id = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(ThreadTasksSubjectParts {
        thread_id: thread_id?,
    })
}

/// Web-server request published for one org's API cluster. In-cluster producers JetStream-publish directly; the org's API replicas bind a durable consumer on the exact subject (not the wildcard). Payload is the web-api envelope in schema/envelope.schema.json. Dedupe with Nats-Msg-Id = envelope.dedupe_key.
/// Service: web-api-data-plane
pub const WEB_API_REQUEST_PATTERN: &str = "dd.remote.web_api.{org_slug}.request";
pub const WEB_API_REQUEST_WILDCARD: &str = "dd.remote.web_api.*.request";
pub const WEB_API_REQUEST_STREAM: &str = "DD_WEB_API_REQUESTS";
pub fn web_api_request_subject(org_slug: &str) -> String {
    format!("dd.remote.web_api.{}.request", org_slug)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct WebApiRequestSubjectParts {
    pub org_slug: String,
}

pub fn parse_web_api_request_subject(subject: &str) -> Option<WebApiRequestSubjectParts> {
    let pattern_tokens: &[&str] = &["dd", "remote", "web_api", "{org_slug}", "request"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut org_slug: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "org_slug" => { org_slug = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(WebApiRequestSubjectParts {
        org_slug: org_slug?,
    })
}

/// Asynchronous status/receipt fan-out for one org after the API consumes a request (pending→published→processing→succeeded|failed|dead_letter). Web servers subscribe to the exact org subject.
/// Service: web-api-data-plane
pub const WEB_API_STATUS_PATTERN: &str = "dd.remote.web_api.{org_slug}.status";
pub const WEB_API_STATUS_WILDCARD: &str = "dd.remote.web_api.*.status";
pub const WEB_API_STATUS_STREAM: &str = "DD_WEB_API_STATUS";
pub fn web_api_status_subject(org_slug: &str) -> String {
    format!("dd.remote.web_api.{}.status", org_slug)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct WebApiStatusSubjectParts {
    pub org_slug: String,
}

pub fn parse_web_api_status_subject(subject: &str) -> Option<WebApiStatusSubjectParts> {
    let pattern_tokens: &[&str] = &["dd", "remote", "web_api", "{org_slug}", "status"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut org_slug: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "org_slug" => { org_slug = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(WebApiStatusSubjectParts {
        org_slug: org_slug?,
    })
}

/// Deliver an external signal to a running workflow. Producers publish to the specific run token; the engine appends the signal to the run and wakes any waitSignal step. Payload is {"name": "approved", "payload"?: any}. Default for NATS_WORKFLOW_SIGNAL_SUBJECT='dd.remote.workflows.signal.*'.
/// Service: dd-gleam-lambda-runner
pub const WORKFLOWS_SIGNAL_PATTERN: &str = "dd.remote.workflows.signal.{run_id}";
pub const WORKFLOWS_SIGNAL_WILDCARD: &str = "dd.remote.workflows.signal.*";
pub const WORKFLOWS_SIGNAL_QUEUE_GROUP: &str = "dd-gleam-workflow-engine";
pub fn workflows_signal_subject(run_id: &str) -> String {
    format!("dd.remote.workflows.signal.{}", run_id)
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct WorkflowsSignalSubjectParts {
    pub run_id: String,
}

pub fn parse_workflows_signal_subject(subject: &str) -> Option<WorkflowsSignalSubjectParts> {
    let pattern_tokens: &[&str] = &["dd", "remote", "workflows", "signal", "{run_id}"];
    let subject_tokens: Vec<&str> = subject.split('.').collect();
    let mut run_id: Option<String> = None;
    let mut si: usize = 0;
    for tok in pattern_tokens.iter() {
        if tok.starts_with('{') && tok.ends_with('}') {
            if si >= subject_tokens.len() { return None; }
            let name = &tok[1..tok.len()-1];
            match name {
                "run_id" => { run_id = Some(subject_tokens[si].to_string()); }
                _ => return None,
            }
            si += 1;
            continue;
        }
        if si >= subject_tokens.len() || subject_tokens[si] != *tok { return None; }
        si += 1;
    }
    if si != subject_tokens.len() { return None; }
    Some(WorkflowsSignalSubjectParts {
        run_id: run_id?,
    })
}

// ---------- Standalone queue groups ----------

/// Shared queue group used by dd-agent-sim-server replicas consuming simulate requests.
/// Service: dd-agent-sim-server
pub const AGENT_SIM_SERVER_QUEUE_GROUP: &str = "dd-agent-sim-server";

/// Queue group shared by dd-billing-server replicas for inbound sync commands so each command is handled by exactly one pod.
/// Service: dd-billing-server
pub const BILLING_SERVER_QUEUE_GROUP: &str = "dd-billing-server";

/// Shared queue group / durable consumer name used by build-server replicas for request intake.
/// Service: dd-build-server
pub const BUILD_SERVER_QUEUE_GROUP: &str = "dd-build-server";

/// Shared queue group used by dd-constraint-scheduler replicas consuming schedule requests.
/// Service: dd-constraint-scheduler
pub const CONSTRAINT_SCHEDULER_QUEUE_GROUP: &str = "dd-constraint-scheduler";

/// Shared queue group for dd-email-sms-contact-rs workers consuming the email/sms send lanes (once-only delivery across replicas).
/// Service: dd-email-sms-contact-rs
pub const CONTACT_SEND_QUEUE_GROUP: &str = "dd-email-sms-contact";

/// Durable queue group used by dd-remote-queue-consumer replicas for critical runtime event logging and future alert fan-out.
/// Service: shared
pub const CRITICAL_EVENTS_LOGGER_QUEUE_GROUP: &str = "dd-runtime-critical-events";

/// Shared queue group used by dd-dataset-labeling replicas so each queued labeling task/annotation request is processed once.
/// Service: dd-dataset-labeling
pub const DATASET_LABELING_WORKERS_QUEUE_GROUP: &str = "dd-dataset-labeling";

/// Shared queue group used by dd-data-viz notifier workers consuming the notification-dispatch lane.
/// Service: dd-data-viz-rs
pub const DATA_VIZ_NOTIFICATION_DISPATCH_QUEUE_GROUP: &str = "dd-data-viz-notifiers";

/// Shared queue group used by document converter replicas so each request is handled once.
/// Service: dd-document-rs
pub const DOCUMENT_CONVERTERS_QUEUE_GROUP: &str = "dd-document-rs";

/// Shared queue group used by dd-economics-server replicas consuming forecast requests.
/// Service: dd-economics-server
pub const ECONOMICS_SERVER_QUEUE_GROUP: &str = "dd-economics-server";

/// Shared queue group used by island worker pods so each per-epoch subpopulation is evolved exactly once.
/// Service: dd-evolution-optimizer
pub const EVOLUTION_ISLANDS_QUEUE_GROUP: &str = "dd-evolution-optimizer-islands";

/// Shared queue group used by dd-func-approx-rs replicas consuming fit requests.
/// Service: dd-func-approx-rs
pub const FUNC_APPROX_QUEUE_GROUP: &str = "dd-func-approx-rs";

/// Shared queue group used by dd-gpu-rs replicas consuming GPU job requests.
/// Service: dd-gpu-rs
pub const GPU_SCHEDULER_QUEUE_GROUP: &str = "dd-gpu-rs";

/// Shared queue group used by dd-knowledge-graph-builder replicas so each queued build request is processed once.
/// Service: dd-knowledge-graph-builder
pub const KNOWLEDGE_GRAPH_WORKERS_QUEUE_GROUP: &str = "dd-knowledge-graph-builder";

/// Shared queue group used by lambda-runner replicas for structured CloudEvents input.
/// Service: dd-gleam-lambda-runner
pub const LAMBDA_EVENTS_QUEUE_GROUP: &str = "dd-gleam-lambda-runner-events";

/// Shared queue group used by lambda-runner replicas.
/// Service: dd-gleam-lambda-runner
pub const LAMBDA_RUNNER_QUEUE_GROUP: &str = "dd-gleam-lambda-runner";

/// Shared queue group used by slave solver pods so each branch-and-bound subproblem is solved once.
/// Service: dd-ai-ml-pipeline
pub const MIP_SOLVER_WORKERS_QUEUE_GROUP: &str = "dd-in-house-mip-solver-node-workers";

/// Shared queue group used by dd-monte-carlo-server replicas consuming simulate requests.
/// Service: dd-monte-carlo-server
pub const MONTE_CARLO_SERVER_QUEUE_GROUP: &str = "dd-monte-carlo-server";

/// Shared queue group used by dd-music-rs replicas consuming generation requests.
/// Service: dd-music-rs
pub const MUSIC_GENERATION_QUEUE_GROUP: &str = "dd-music-rs";

/// Shared queue group used by OCR replicas so each request is handled once.
/// Service: dd-ocr-rs
pub const OCR_WORKERS_QUEUE_GROUP: &str = "dd-ocr-rs";

/// Shared queue group used by dd-public-data-server replicas so each queued ingest/scrape request is processed once.
/// Service: dd-public-data-server
pub const PUBLIC_DATA_WORKERS_QUEUE_GROUP: &str = "dd-public-data-server";

/// Queue group shared by Quaestor web-server replicas for opt-in ledger sync commands.
/// Service: dd-quaestor-web-server
pub const QUAESTOR_WEB_SERVER_QUEUE_GROUP: &str = "dd-quaestor-web-server";

/// Shared queue group used by dd-quantum-compute-rs replicas consuming solve requests.
/// Service: dd-quantum-compute-rs
pub const QUANTUM_QUEUE_GROUP: &str = "dd-quantum-compute-rs";

/// Shared queue group used by dd-raft-consensus replicas consuming propose/scenario requests.
/// Service: dd-raft-consensus
pub const RAFT_CONSENSUS_QUEUE_GROUP: &str = "dd-raft-consensus";

/// Shared queue group used by dd-route-optimizer replicas consuming optimize requests.
/// Service: dd-route-optimizer
pub const ROUTE_OPTIMIZER_QUEUE_GROUP: &str = "dd-route-optimizer";

/// Shared queue group used by routing worker pods so each multi-start restart is solved exactly once.
/// Service: dd-routing-server
pub const ROUTING_WORKERS_QUEUE_GROUP: &str = "dd-routing-server-workers";

/// Shared queue group used by dd-sat-smt-server replicas consuming solve requests.
/// Service: dd-sat-smt-server
pub const SAT_SMT_SERVER_QUEUE_GROUP: &str = "dd-sat-smt-server";

/// Shared queue group used by dd-remote-queue-consumer replicas so each task is only prepared once.
/// Service: dd-remote-rest-api
pub const THREAD_PREPARER_QUEUE_GROUP: &str = "dd-remote-thread-preparer";

/// Shared queue group used by workflow-engine replicas for run start and signal delivery.
/// Service: dd-gleam-lambda-runner
pub const WORKFLOW_ENGINE_QUEUE_GROUP: &str = "dd-gleam-workflow-engine";

// ---------- JetStream streams ----------

/// Default JetStream stream for the wal-gateway publish path. Overridable via WAL_GATEWAY_STREAM_NAME.
/// Service: dd-wal-gateway
pub const CDC_STREAM_NAME: &str = "CDC";
pub const CDC_STREAM_SUBJECTS: &[&str] = &["cdc.>"];
pub const CDC_STREAM_RETENTION: &str = "limits";
pub const CDC_STREAM_STORAGE: &str = "file";
pub const CDC_STREAM_ACK: &str = "explicit";

/// JetStream file storage with WorkQueue retention and explicit ack for build-request intake. Dedupe by Nats-Msg-Id ('build-request:<requestId>'); Postgres (dd_build_server) remains the real idempotency guard.
/// Service: dd-build-server
pub const DD_REMOTE_BUILD_JOBS_STREAM_NAME: &str = "DD_REMOTE_BUILD_JOBS";
pub const DD_REMOTE_BUILD_JOBS_STREAM_SUBJECTS: &[&str] = &["dd.remote.build_server.requests"];
pub const DD_REMOTE_BUILD_JOBS_STREAM_RETENTION: &str = "workqueue";
pub const DD_REMOTE_BUILD_JOBS_STREAM_STORAGE: &str = "file";
pub const DD_REMOTE_BUILD_JOBS_STREAM_ACK: &str = "explicit";

/// Short-retention control plane stream.
/// Service: dd-remote-rest-api
pub const DD_REMOTE_CONTROL_STREAM_NAME: &str = "DD_REMOTE_CONTROL";
pub const DD_REMOTE_CONTROL_STREAM_SUBJECTS: &[&str] = &["dd.remote.thread.*.control", "dd.remote.orchestrator.wakeup"];
pub const DD_REMOTE_CONTROL_STREAM_RETENTION: &str = "limits";
pub const DD_REMOTE_CONTROL_STREAM_STORAGE: &str = "file";
pub const DD_REMOTE_CONTROL_STREAM_ACK: &str = "explicit";

/// Durable file-backed stream for alert-worthy runtime failures. Consumers log and alert from this stream; payloads must stay compact and redacted.
/// Service: shared
pub const DD_REMOTE_CRITICAL_EVENTS_STREAM_NAME: &str = "DD_REMOTE_CRITICAL_EVENTS";
pub const DD_REMOTE_CRITICAL_EVENTS_STREAM_SUBJECTS: &[&str] = &["dd.remote.events.critical"];
pub const DD_REMOTE_CRITICAL_EVENTS_STREAM_RETENTION: &str = "limits";
pub const DD_REMOTE_CRITICAL_EVENTS_STREAM_STORAGE: &str = "file";
pub const DD_REMOTE_CRITICAL_EVENTS_STREAM_ACK: &str = "explicit";

/// Cron-initiated jobs.
/// Service: dd-remote-rest-api
pub const DD_REMOTE_CRON_STREAM_NAME: &str = "DD_REMOTE_CRON";
pub const DD_REMOTE_CRON_STREAM_SUBJECTS: &[&str] = &["dd.remote.cron.prompts"];
pub const DD_REMOTE_CRON_STREAM_RETENTION: &str = "limits";
pub const DD_REMOTE_CRON_STREAM_STORAGE: &str = "file";
pub const DD_REMOTE_CRON_STREAM_ACK: &str = "explicit";

/// Longer-retention event stream for replay/debugging, mirrored into Postgres.
/// Service: dd-remote-rest-api
pub const DD_REMOTE_EVENTS_STREAM_NAME: &str = "DD_REMOTE_EVENTS";
pub const DD_REMOTE_EVENTS_STREAM_SUBJECTS: &[&str] = &["dd.remote.thread.*.events", "dd.remote.thread.*.heartbeat"];
pub const DD_REMOTE_EVENTS_STREAM_RETENTION: &str = "limits";
pub const DD_REMOTE_EVENTS_STREAM_STORAGE: &str = "file";
pub const DD_REMOTE_EVENTS_STREAM_ACK: &str = "explicit";

/// JetStream stream for distributed island-model GA jobs, results, ring migrants, and progress events.
/// Service: dd-evolution-optimizer
pub const DD_REMOTE_EVOLUTION_STREAM_NAME: &str = "DD_REMOTE_EVOLUTION";
pub const DD_REMOTE_EVOLUTION_STREAM_SUBJECTS: &[&str] = &["dd.remote.evolution.jobs", "dd.remote.evolution.results", "dd.remote.evolution.migrants", "dd.remote.evolution.events"];
pub const DD_REMOTE_EVOLUTION_STREAM_RETENTION: &str = "limits";
pub const DD_REMOTE_EVOLUTION_STREAM_STORAGE: &str = "file";
pub const DD_REMOTE_EVOLUTION_STREAM_ACK: &str = "explicit";

/// Durable JetStream history for fabrication requests, results, machine profiles, design conversion, instruction generation and review, execution telemetry, learning outcomes, and release readiness.
/// Service: dd-fabrication-server
pub const DD_REMOTE_FABRICATION_STREAM_NAME: &str = "DD_REMOTE_FABRICATION";
pub const DD_REMOTE_FABRICATION_STREAM_SUBJECTS: &[&str] = &["dd.remote.fabrication.>"];
pub const DD_REMOTE_FABRICATION_STREAM_RETENTION: &str = "limits";
pub const DD_REMOTE_FABRICATION_STREAM_STORAGE: &str = "file";
pub const DD_REMOTE_FABRICATION_STREAM_ACK: &str = "explicit";

/// JetStream stream for distributed in-house LP/MIP/IP solver work, results, control, and progress events.
/// Service: dd-ai-ml-pipeline
pub const DD_REMOTE_MIP_SOLVER_STREAM_NAME: &str = "DD_REMOTE_MIP_SOLVER";
pub const DD_REMOTE_MIP_SOLVER_STREAM_SUBJECTS: &[&str] = &["dd.remote.mip_solver.jobs", "dd.remote.mip_solver.results", "dd.remote.mip_solver.control", "dd.remote.mip_solver.events"];
pub const DD_REMOTE_MIP_SOLVER_STREAM_RETENTION: &str = "limits";
pub const DD_REMOTE_MIP_SOLVER_STREAM_STORAGE: &str = "file";
pub const DD_REMOTE_MIP_SOLVER_STREAM_ACK: &str = "explicit";

/// JetStream stream for distributed VRP/TSP solve jobs, worker tour results, and incumbent-improvement events.
/// Service: dd-routing-server
pub const DD_REMOTE_ROUTING_STREAM_NAME: &str = "DD_REMOTE_ROUTING";
pub const DD_REMOTE_ROUTING_STREAM_SUBJECTS: &[&str] = &["dd.remote.routing.jobs", "dd.remote.routing.results", "dd.remote.routing.events"];
pub const DD_REMOTE_ROUTING_STREAM_RETENTION: &str = "limits";
pub const DD_REMOTE_ROUTING_STREAM_STORAGE: &str = "file";
pub const DD_REMOTE_ROUTING_STREAM_ACK: &str = "explicit";

/// JetStream file storage with WorkQueue retention, explicit ack, and message dedupe by Nats-Msg-Id ('remote-task:<taskId>'). Postgres remains the real idempotency guard.
/// Service: dd-remote-rest-api
pub const DD_REMOTE_TASKS_STREAM_NAME: &str = "DD_REMOTE_TASKS";
pub const DD_REMOTE_TASKS_STREAM_SUBJECTS: &[&str] = &["dd.remote.thread.*.tasks"];
pub const DD_REMOTE_TASKS_STREAM_RETENTION: &str = "workqueue";
pub const DD_REMOTE_TASKS_STREAM_STORAGE: &str = "file";
pub const DD_REMOTE_TASKS_STREAM_ACK: &str = "explicit";

/// Durable limits-retention stream for redacted terminal task failures. It is separate from DD_REMOTE_TASKS so dead letters cannot inflate queue-consumer lag or trigger KEDA scaling.
/// Service: dd-remote-rest-api
pub const DD_REMOTE_TASKS_DLQ_STREAM_NAME: &str = "DD_REMOTE_TASKS_DLQ";
pub const DD_REMOTE_TASKS_DLQ_STREAM_SUBJECTS: &[&str] = &["dd.remote.thread.tasks.deadletter"];
pub const DD_REMOTE_TASKS_DLQ_STREAM_RETENTION: &str = "limits";
pub const DD_REMOTE_TASKS_DLQ_STREAM_STORAGE: &str = "file";
pub const DD_REMOTE_TASKS_DLQ_STREAM_ACK: &str = "explicit";

/// Work-queue JetStream intake for web→API envelopes. Each org's API server binds durable name dd-web-api-{org_slug} on the exact request subject. Dedupe by Nats-Msg-Id. File storage, explicit ack.
/// Service: web-api-data-plane
pub const DD_WEB_API_REQUESTS_STREAM_NAME: &str = "DD_WEB_API_REQUESTS";
pub const DD_WEB_API_REQUESTS_STREAM_SUBJECTS: &[&str] = &["dd.remote.web_api.*.request"];
pub const DD_WEB_API_REQUESTS_STREAM_RETENTION: &str = "workqueue";
pub const DD_WEB_API_REQUESTS_STREAM_STORAGE: &str = "file";
pub const DD_WEB_API_REQUESTS_STREAM_ACK: &str = "explicit";

/// Limits-retention fan-out of per-org asynchronous receipts. Not a work queue so every interested web replica can observe status.
/// Service: web-api-data-plane
pub const DD_WEB_API_STATUS_STREAM_NAME: &str = "DD_WEB_API_STATUS";
pub const DD_WEB_API_STATUS_STREAM_SUBJECTS: &[&str] = &["dd.remote.web_api.*.status"];
pub const DD_WEB_API_STATUS_STREAM_RETENTION: &str = "limits";
pub const DD_WEB_API_STATUS_STREAM_STORAGE: &str = "file";
pub const DD_WEB_API_STATUS_STREAM_ACK: &str = "explicit";
